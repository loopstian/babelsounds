"I am implementing Step 2 of the **Dynamic RAG system**. Now, when the Gatekeeper decides a search is needed, the backend must execute the search and feed the results to the ElevenLabs Agent.

**1. Update the `POST /api/interrogate` route in `server.js`:**
- If the Gatekeeper's `action` is `'SEARCH'`:
    - **Step A: Firecrawl Search.** Use the `firecrawl` client to search for the `query` generated by the Gatekeeper. Limit to 1 result and use `scrapeOptions: { formats: ['markdown'] }`.
    - **Step B: Summarize the Fragment.** Pass the resulting Markdown to `gemini-2.5-flash-lite`. 
      *System Instruction:* 'You are a neural processor. Summarize the following web data into a 2-3 sentence "Memory Fragment" that directly answers the user's query about the [LanguageName] culture. Focus on historical facts, specific recipes, or poetic structures found in the text.'
    - **Step C: Construct the Injected Message.** Create a new string for the ElevenLabs Agent:
      `[USER MESSAGE]: ${userMessage}\n\n[RECOVERED MEMORY]: ${memoryFragment}\n\n[INSTRUCTION]: You just remembered this from your past life. Use these facts to answer the user, but stay 100% in your cryptic, ancient persona. Speak only in your phonetic language.`

**2. Execute the Interrogation:**
- Send this new 'Injected Message' to the ElevenLabs Agent (the `interrogate` logic we built previously).
- Ensure the final response sent to the React frontend includes:
    - `audioBase64`: The generated voice.
    - `english`: The translated subtitle.
    - `phonetic`: The spoken gibberish.
    - `telemetry`: A new field containing the `memoryFragment` we recovered.

**3. Frontend Telemetry UI (Screen 3):**
- Update the UI in **Screen 3**. When a response with `telemetry` arrives:
    - Briefly display a new terminal line above the subtitles: `> MEMORY RECOVERED: [The first 5 words of the fragment]...`
    - This gives the user visual confirmation that the entity's answer is based on a real search.

**4. Error Handling:**
- If Firecrawl fails or returns no results, fall back to a standard 'TALK' action so the conversation doesn't crash.

**Please wire up the Firecrawl-to-ElevenLabs pipeline so the entity can now answer questions about its history and culture using live web data.**"